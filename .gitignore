# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Node (caso adicionem no futuro)
node_modules/
npm-debug.log

# Env (nunca commitar)
.env
.env.local
.env.production

# Firebase Service Account (NUNCA commitar)
scripts/serviceAccountKey.json
**/serviceAccountKey.json

# Test files
test/*
*.test.js
# Exceção: harness de testes das Firestore Rules é versionado (CLAUDE.md exige
# rodar testes do emulator antes de deploy de rules). node_modules fica de fora.
!test/rules/
!test/rules/**
test/rules/node_modules/

# Build (projeto é estático — sem build)
dist/
build/

# Claude Code sessions
.claude/

# Firebase debug
firebase-debug.log
firestore-debug.log
ui-debug.log

# Local Netlify folder
.netlify
