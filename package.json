{
  "name": "encontrepet-root",
  "private": true,
  "scripts": {
    "dev": "firebase emulators:start --only hosting",
    "serve": "firebase serve --only hosting",
    "deploy:functions": "firebase deploy --only functions",
    "deploy": "firebase deploy",
    "test:rules": "firebase emulators:exec --only firestore --project encontrepet-rules-test \"npm --prefix test/rules test\""
  }
}
